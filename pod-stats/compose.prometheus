services:
  prometheus:
    image: docker.io/prom/prometheus:latest
    container_name: prometheus
    network_mode: "host"
    volumes:
      - /home/ubuntu/container_apps/pod-stats/prometheus.yml:/etc/prometheus/prometheus.yml
    restart: always
